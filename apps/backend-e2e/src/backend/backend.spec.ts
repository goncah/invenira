//import axios from 'axios';

describe('GET /api', () => {
  it('should return a message', async () => {
    //const res = await axios.get(`/activity-providers`);

    expect(200).toBe(200);
  });
});
